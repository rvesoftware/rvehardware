import Layout from "../components/Layout";

export default function Error() {
  return(
    <Layout>
        <h2>OOPS!</h2>
        <p>La pagina que solicitaste no funciona</p>
        <button>Volver al Inicio</button>
    </Layout>
  );
}
